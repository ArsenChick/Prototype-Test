language: cpp
dist: bionic
compiler: gcc

before_install:
  - pip install --user cpp-coveralls

addons:
  sonarcloud:
    organization: "doomfister"

script:
  - qmake
  - make
  - sudo ldconfig
  - LD_LIBRARY_PATH=$$PWD/../external/SFML-2.5.1/lib:$LD_LIBRARY_PATH
  - export LD_LIBRARY_PATH
  - ./tests/tests

after_success:
  - coveralls --root . -E ".*external.*" -E ".*tests.*"
  - build-wrapper-linux-x86-64 --out-dir bw-output make clean all
  - sonar-scanner -Dsonar.cfamily.build-wrapper-output=bw-output

cache:
  directories:
  - '$HOME/.sonar/cache'

notifications:
  email: false
